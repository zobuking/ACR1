<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acrv00">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".floor_main"></activity>
        <activity android:name=".map_foor_main" />
        <activity android:name=".schedule_main" />
        <activity android:name=".battery_main" />
        <activity android:name=".settings_main" />
        <activity android:name=".mute_main" />
        <activity android:name=".notification_main" />
        <activity android:name=".alarm_main" />
        <activity android:name=".sleep_main" />
        <activity android:name=".alert_main" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>